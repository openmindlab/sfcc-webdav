export enum OutputDir {
  CODE = 'output/code',
  CONFIG = 'output/config'
}
export enum ArchiveName {
  METADATA = 'metadata.zip',
  DEMO = 'site-demo.zip',
  TEMPLATE = 'site-template.zip',
  EMPTY = 'empty.zip',
  CODE = '.zip'
}
